#include "tests.h"
#include "playscale.h"
/*
tests[TEST_count] = {
  &playscale,
  &playscale,
  &playscale,
  &playscale,
  &playscale,
}

test = tests[TEST_AUDIBLE];

void test_select(test_module t) {
  asm('sei');
  test = tests[t];
  asm('cli');
}

void test_start() {
  test_state = 1;
  test(1);
}

void test_stop() {
  test_state = 0;
  test(-1);
}
*/
